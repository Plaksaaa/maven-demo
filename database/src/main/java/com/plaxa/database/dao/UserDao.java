package com.plaxa.database.dao;

import com.plaxa.database.entity.User;

import java.util.Optional;

public class UserDao {
     public Optional<User> findById(Long id){
          return Optional.empty();
     }
}
